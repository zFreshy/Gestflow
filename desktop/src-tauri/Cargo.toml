[package]
name = "mercadinho"
version = "0.1.0"
description = "Mercadinho - PDV e controle de estoque"
authors = ["zFreshy"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "mercadinho_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
tauri-plugin-store = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Impressao direta na bobina passa pelo spooler do Windows (ver src/printer.rs).
# `windows-sys` e nao `windows`: sao ligacoes cruas, sem camada de tipos por
# cima, o que deixa a chamada igual a documentacao da Microsoft e evita
# surpresa de assinatura a cada versao.
[target.'cfg(windows)'.dependencies]
# Gdi e Security entram porque o PRINTER_INFO_2W depende dos dois — e e ele que
# traz porta e driver, o unico jeito de saber se a impressora e de verdade ou
# virtual (Print to PDF, XPS).
windows-sys = { version = "0.59", features = [
  "Win32_Foundation",
  "Win32_Graphics_Gdi",
  "Win32_Graphics_Printing",
  "Win32_Security",
] }

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
